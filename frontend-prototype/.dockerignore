# Build inputs - Docker excludes these from COPY automatically, but being
# explicit keeps them out of the served web root.
Dockerfile
.dockerignore

# Editor and OS noise.
.vscode/
.idea/
Thumbs.db
.DS_Store
